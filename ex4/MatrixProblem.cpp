#include "MatrixProblem.h"

//create the matrix from lines of string
MatrixProblem::MatrixProblem(std::vector<std::vector<int>> lines, std::pair<int,int> initial, std::pair<int,int> goal ) {
	//convert the vector of vectors to int**

}
//TODO
std::vector<State<std::pair<int, int>>*> MatrixProblem::getAllNeighbors(State<std::pair<int, int>>* state) {

}